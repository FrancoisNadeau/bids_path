---
# European data format
- suffixes:
  - ieeg
  extensions:
  - .json
  - .edf
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

# BrainVision Core Data Format
- suffixes:
  - ieeg
  extensions:
  - .json
  - .vhdr
  - .eeg
  - .vmrk
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

# EEGLAB
- suffixes:
  - ieeg
  extensions:
  - .json
  - .set
  - .fdt
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

# Neurodata Without Borders
- suffixes:
  - ieeg
  extensions:
  - .json
  - .nwb
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

# MEF3
- suffixes:
  - ieeg
  extensions:
  - .mefd/
  - .json
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

# Channels description
- suffixes:
  - channels
  extensions:
  - .json
  - .tsv
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

# Coordinate system
- suffixes:
  - coordsystem
  extensions:
  - .json
  entities:
    subject: required
    session: optional
    acquisition: optional
    space: optional

# Electrode description
- suffixes:
  - electrodes
  extensions:
  - .json
  - .tsv
  entities:
    subject: required
    session: optional
    acquisition: optional
    space: optional

# Events
- suffixes:
  - events
  extensions:
  - .json
  - .tsv
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

# Electrode position photos
- suffixes:
  - photo
  extensions:
  - .jpg
  entities:
    subject: required
    session: optional
    acquisition: optional

# Timeseries
- suffixes:
  - physio
  - stim
  extensions:
  - .tsv.gz
  - .json
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    recording: optional
